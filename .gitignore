# Local Netlify folder
.netlify
.DS_Store
lighthouse-*.json

# Credenciais e variaveis de ambiente — NUNCA commitar
.env
.env.*
*.env
!.env.example
.mp-credentials.txt
.mp-plans.json

# Relatorios de medicao (Lighthouse) — lixo de trabalho
lh-*.json
